OmniForge Windows verification exit code: 1
Generated: 2026-07-25T14:14:26.7169184+00:00

--- STDOUT ---

> omniforge-3d-engine@0.11.0 verify
> node scripts/verify.mjs

Syntax: server: passed
Syntax: provider framework: passed
Syntax: job manager: passed
Syntax: marketplace: passed
Syntax: local worker: passed
Syntax: MCP bridge: passed
Syntax: editor: passed
Syntax: runtime diagnostics: passed
Syntax: renderer: passed
Syntax: desktop shell: passed
✔ automatic world time cannot replace the editor workspace while authoring (1.423ms)
✖ compact world runtime patches lighting and celestial objects without replacing editor state (12.8912ms)
✖ night remains readable in Edit mode without changing authored Play lighting (61.3859ms)
✔ runtime source contains polling, compact-step, read-only GET, and traceable-build safeguards (3.0017ms)
✔ default project is general-purpose and contains a real 3D starter scene (17.8802ms)
✔ editor defaults include persistent layouts, shortcuts, recovery state, and camera preferences (4.9318ms)
✔ all supported scene primitives receive valid transforms and properties (2.9148ms)
✔ terrain height is deterministic and responds to world position (0.8979ms)
✔ path defaults preserve terrain-conforming system relationships (0.2689ms)
✔ terrain path mask produces a soft shoulder instead of a hard ribbon (0.6999ms)
✔ starter PBR materials include real texture-map files and are protected (4.9828ms)
✔ horizontal mouse look uses the intuitive right-positive yaw convention (0.5774ms)
✔ desktop lifecycle implements native isolation, recovery, safe mode, and clean process ownership (1.0449ms)
✔ Windows desktop builder pins and stamps the native executable metadata (1.0803ms)
✔ project hub and editor usability controls are wired into the authoritative editor (3.47ms)
✔ project store supports create, open, duplicate, archive, import, migration, and moved-project recovery (584.5142ms)
✔ two runtime sessions cannot write the same active project simultaneously (748.5318ms)
✔ starter objects are grounded against the authoritative terrain surface (2.4619ms)
✔ browser automation bridge exposes read-only snapshots and controlled editor actions (1.108ms)
✔ managed workspace path cannot escape the projects root (0.939ms)
✔ Codex MCP initializes and exposes scene, material, prefab, and project lifecycle tools (220.1123ms)
✔ Codex can import and rebuild a hierarchy-aware model through guarded MCP tools (540.8407ms)
✔ material authoring settings clamp safely and preserve tiling transforms (0.2456ms)
✔ material tuner exposes live tiling and PBR response controls (1.2511ms)
✔ active source and documentation contain no legacy product assumptions (12.7312ms)
✔ desktop clipboard and pointer-lock permissions use guarded native paths (0.7438ms)
✔ surface recipes are stable linked assets with validation and deterministic masks (3.1475ms)
✔ Surface Studio preview, commit, revert, and recipe rendering are wired (0.7785ms)
✔ desktop launcher rejects stale packaged executables (0.1873ms)
✔ canonical GLB pipeline preserves source and produces health, render, collision, LOD, and repair derivatives (16.595ms)
✔ asset workspace exposes real import, health, reversible processing, preview, and commit flows (1.9369ms)
✔ asset recipes remain synchronized with canonical models, collision, LODs, provenance, and usages (14.4262ms)
✔ real asset API imports, recipes, processes, previews, commits, and persists a GLB through the authoritative server (553.5625ms)
✔ canonical importer applies nested glTF node transforms and preserves primitive material groups (7.8844ms)
✔ canonical rebuild repairs legacy imports from preserved source and retains rollback history (11.7659ms)
✔ asset workspace reflows into focused subviews and protects the viewport from panel overlap (1.2487ms)
✔ provider framework normalizes independent providers, settings, and persistent job records (3.1622ms)
✔ Integrations workspace and Job Center are connected to guarded provider and worker routes (1.7072ms)
✔ real provider health, project validation, cancellation, retry, and restart-safe jobs run through isolated workers (2854.2509ms)
✔ v0.8 marketplace workspace, providers, staged downloads, and guarded import are wired into the authoritative editor (5.3159ms)
✔ mocked Poly Haven search completes a real staged download job and imports a canonical model (1159.9235ms)
✔ ambientCG provider migrates existing v2 settings to the supported v3 asset API (1.0483ms)
✔ v0.9 Surface Recipe v2 normalizes advanced masks, graph data, colors, and deterministic compilation (1.1251ms)
✔ v0.9 decal and atlas recipes validate stable production metadata (0.648ms)
✔ v0.9 Production Surface Studio exposes map processing, advanced graph, decals, atlases, and guarded tools (1.8294ms)
✔ v0.9 real API creates processed material derivatives, compiles recipes, creates decals, and persists atlas layouts (512.4836ms)
✔ Git-aware desktop launcher rebuilds after source commits and local edits (1.2315ms)
✔ Phase 0A collapses legacy celestial duplicates into one protected Sun and one protected Moon (3.2633ms)
✔ Phase 0A preserves a selectable persistent Moon identity across repeated repairs (1.3537ms)
✔ Phase 0B interpolates celestial rotations through the shortest angular path (1.1088ms)
✔ Phase 0C editor readability no longer invents a white directional Sun or forces high exposure (12.5448ms)
✖ Phase 0 source contracts persist proxies, protect selection, smooth motion, and expose diagnostics (4.024ms)
✔ Celestial Studio settings normalize and create authoritative Sun and Moon entities (5.3902ms)
✔ renderer environment exposes adjustable discs, detailed moon, planet, moonlight, and volumetric cloud inputs (0.6622ms)
✔ terrain expansion preserves physical vertex density until the single-mesh safety ceiling (1.2809ms)
✔ local terrain sculpt stamps are reversible and do not alter distant samples (2.4558ms)
✔ World, path, terrain, renderer, and API source expose the corrective authoring contracts (3.1935ms)
✔ sky rays rotate with the camera but ignore camera translation (3.9031ms)
✔ environment state is bounded and shares the directional light authority (1.0373ms)
✔ nested World settings drive weather, cloud wind, quality, and twilight (0.3596ms)
✔ viewport look ignores acquisition noise and rejects implausible direction snaps (0.76ms)
✔ yaw remains normalized and pitch remains bounded (0.2351ms)
✔ viewport acquisition protects camera authority before pointer lock and releases cleanly (1.4311ms)
✔ normal rendering no longer depends on the legacy CSS atmosphere (1.5482ms)
✔ v0.10 worker IPC drains stdout before finalizing jobs (1.8441ms)
✔ v0.10 seeded random and foliage placement are deterministic (24.3399ms)
✔ v0.10 world system drives the authoritative scene sun and renderer settings (2.3253ms)
✔ v0.10 support-plane grounding conforms a box to terrain at four corners (0.8688ms)
✔ v0.10 foliage uses root-socket grounding and vehicles remain upright (0.8304ms)
✔ v0.10 cyclic glTF hierarchy fails safely without recursive stack overflow (24.409ms)
✔ v0.10 embedded glTF texture is extracted and linked to authored UV material (13.4ms)
✔ v0.10 desktop package, UI, and MCP share the connected authorities (1.0899ms)
✔ v0.10 atmosphere controls affect lighting weather surfaces and optimized foliage rendering (1.4294ms)
✔ v0.10 existing Ground button and World tab synchronize through authoritative state (1.5116ms)
✔ v0.10 bootstrap serves world systems and upgrades the existing Ground command (936.9087ms)
✔ Scene Block inspector enhancement has a stable marker and coalesced observer (1.5269ms)
✔ diagnostic mode includes input, event-loop, WebGL, and long-task evidence (1.0812ms)
✔ v0.11 renderer constructs a finite terrain mesh without missing runtime helpers (20.8043ms)
✔ v0.11 terrain generation is deterministic, seeded, and offers requested landform presets (19.4181ms)
✔ v0.11 migration converts transform scaling into explicit bounds and world-space path nodes (1.1016ms)
✔ expanding terrain bounds preserves old terrain samples and path coordinates exactly (1.1718ms)
✔ spline sampling, insertion, and splitting preserve a smooth connected world-space path (2.3066ms)
✔ path grade compilation respects configured maximum grade and bounded terrain cut/fill (15.6138ms)
✔ path material blending follows the sampled spline instead of straight control-point chords (1.2102ms)
✔ v0.11 editor, renderer, runtime, desktop, and MCP expose the connected world foundation (3.2877ms)
✔ v0.11 bootstrap persists terrain expansion and spline node editing through real APIs (832.523ms)
✔ same-scene live viewport camera remains authoritative during navigation and pending persistence (2.012ms)
✔ path guide edges share exact joined endpoints and follow edge terrain height (0.9211ms)
ℹ tests 88
ℹ suites 0
ℹ pass 85
ℹ fail 3
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 7632.1241

✖ failing tests:

test at tests\editor-runtime-stability.test.mjs:23:1
✖ compact world runtime patches lighting and celestial objects without replacing editor state (12.8912ms)
  AssertionError [ERR_ASSERTION]: Expected values to be strictly equal:
  
  1 !== 0.05
  
      at TestContext.<anonymous> (file:///D:/a/OmniForge/OmniForge/tests/editor-runtime-stability.test.mjs:66:10)
      at Test.runInAsyncScope (node:async_hooks:227:14)
      at Test.run (node:internal/test_runner/test:1325:25)
      at Test.processPendingSubtests (node:internal/test_runner/test:911:18)
      at Test.postRun (node:internal/test_runner/test:1465:19)
      at Test.run (node:internal/test_runner/test:1390:12)
      at async startSubtestAfterBootstrap (node:internal/test_runner/harness:385:3) {
    generatedMessage: true,
    code: 'ERR_ASSERTION',
    actual: 1,
    expected: 0.05,
    operator: 'strictEqual',
    diff: 'simple'
  }

test at tests\editor-runtime-stability.test.mjs:71:1
✖ night remains readable in Edit mode without changing authored Play lighting (61.3859ms)
  AssertionError [ERR_ASSERTION]: The expression evaluated to a falsy value:
  
    assert.ok(edit.ambientIntensity >= 0.42)
  
      at TestContext.<anonymous> (file:///D:/a/OmniForge/OmniForge/tests/editor-runtime-stability.test.mjs:74:10)
      at Test.runInAsyncScope (node:async_hooks:227:14)
      at Test.run (node:internal/test_runner/test:1325:25)
      at Test.processPendingSubtests (node:internal/test_runner/test:911:18)
      at Test.postRun (node:internal/test_runner/test:1465:19)
      at Test.run (node:internal/test_runner/test:1390:12)
      at async Test.processPendingSubtests (node:internal/test_runner/test:911:7) {
    generatedMessage: true,
    code: 'ERR_ASSERTION',
    actual: false,
    expected: true,
    operator: '==',
    diff: 'simple'
  }

test at tests\phase0-celestial-stabilization.test.mjs:94:1
✖ Phase 0 source contracts persist proxies, protect selection, smooth motion, and expose diagnostics (4.024ms)
  AssertionError [ERR_ASSERTION]: The input did not match the regular expression /world-read-migration/. Input:
  
  "import fs from 'node:fs';\r\n" +
    "import path from 'node:path';\r\n" +
    'import {\r\n' +
    '  readState,\r\n' +
    '  mutateState,\r\n' +
    '  addActivity,\r\n' +
    '  activeScene,\r\n' +
    '  findObject,\r\n' +
    '  createSceneObject,\r\n' +
    '  ASSET_ROOT\r\n' +
    "} from './state-store.mjs';\r\n" +
    'import {\r\n' +
    '  defaultWorldSettings,\r\n' +
    '  applyWorldToScene,\r\n' +
    '  fitGroundContact,\r\n' +
    '  generateFoliagePlacements\r\n' +
    "} from './v010-systems.mjs';\r\n" +
    '\r\n' +
    'const now = () => new Date().toISOString();\r\n' +
    '\r\n' +
    'function json(res, status, payload) {\r\n' +
    '  const responseBody = JSON.stringify(payload, null, 2);\r\n' +
    '  res.writeHead(status, {\r\n' +
    "    'content-type': 'application/json; charset=utf-8',\r\n" +
    "    'content-length': Buffer.byteLength(responseBody),\r\n" +
    "    'cache-control': 'no-store'\r\n" +
    '  });\r\n' +
    '  res.end(responseBody);\r\n' +
    '}\r\n' +
    '\r\n' +
    '\r\n' +
    'function compactWorldRuntime(state) {\r\n' +
    '  const scene = activeScene(state);\r\n' +
    '  return {\r\n' +
    '    engineRevision: Number(state.engine?.revision || 0),\r\n' +
    '    sceneId: scene.id,\r\n' +
    '    settings: structuredClone(scene.settings || {}),\r\n' +
    '    celestialObjects: scene.objects\r\n' +
    "      .filter(object => object.type === 'directionalLight' || object.properties?.celestialRole)\r\n" +
    '      .map(object => ({\r\n' +
    '        id: object.id,\r\n' +
    '        type: object.type,\r\n' +
    '        visible: object.visible !== false,\r\n' +
    '        transform: structuredClone(object.transform),\r\n' +
    '        properties: structuredClone(object.properties || {})\r\n' +
    '      }))\r\n' +
    '  };\r\n' +
    '}\r\n' +
    '\r\n' +
    'async function readJsonBody(req) {\r\n' +
    '  const chunks = [];\r\n' +
    '  let size = 0;\r\n' +
    '  for await (const chunk of req) {\r\n' +
    '    size += chunk.length;\r\n' +
    "    if (size > 20_000_000) throw new Error('The v0.10 request body exceeds the 20 MB safety limit.');\r\n" +
    '    chunks.push(chunk);\r\n' +
    '  }\r\n' +
    "  return chunks.length ? JSON.parse(Buffer.concat(chunks).toString('utf8')) : {};\r\n" +
    '}\r\n' +
    '\r\n' +
    'function ensureWorld(state) {\r\n' +
    '  state.worldV010 = defaultWorldSettings(state.worldV010 || {});\r\n' +
    '  applyWorldToScene(activeScene(state), state.worldV010);\r\n' +
    '  return state.worldV010;\r\n' +
    '}\r\n' +
    '\r\n' +
    'function findModelAsset(state, assetId) {\r\n' +
    "  const asset = (state.assets || []).find(item => item.id === assetId && item.type === 'model');\r\n" +
    "  if (!asset) throw new Error('Model asset not found.');\r\n" +
    '  return asset;\r\n' +
    '}\r\n' +
    '\r\n' +
    'function assetUsages(state, assetId) {\r\n' +
    '  const sceneUsages = [];\r\n' +
    '  for (const scene of state.scenes || []) {\r\n' +
    '    for (const object of scene.objects || []) {\r\n' +
    '      if (object.properties?.assetId === assetId) {\r\n' +
    '        sceneUsages.push({ sceneId: scene.id, sceneName: scene.name, objectId: object.id, objectName: object.name });\r\n' +
    '      }\r\n' +
    '    }\r\n' +
    '  }\r\n' +
    '  const dependencies = (state.assets || [])\r\n' +
    '    .filter(item => item.id !== assetId && (\r\n' +
    '      item.sourceAssetId === assetId\r\n' +
    '      || item.canonicalAssetId === assetId\r\n' +
    '      || item.baseAssetId === assetId\r\n' +
    '      || item.sourceModelAssetId === assetId\r\n' +
    '      || item.sourceAssetIds?.includes?.(assetId)\r\n' +
    '    ))\r\n' +
    '    .map(item => ({ id: item.id, type: item.type, name: item.name }));\r\n' +
    '  return { sceneUsages, dependencies };\r\n' +
    '}\r\n' +
    '\r\n' +
    'function moveManagedAssetToTrash(assetId) {\r\n' +
    "  const modelsRoot = path.resolve(ASSET_ROOT, 'models');\r\n" +
    '  const source = path.resolve(modelsRoot, assetId);\r\n' +
    "  if (!source.startsWith(`${modelsRoot}${path.sep}`)) throw new Error('Refusing to delete outside the managed model directory.');\r\n" +
    '  if (!fs.existsSync(source)) return null;\r\n' +
    "  const trashRoot = path.resolve(ASSET_ROOT, '.trash');\r\n" +
    '  fs.mkdirSync(trashRoot, { recursive: true });\r\n' +
    '  const target = path.join(trashRoot, `${assetId}-${Date.now().toString(36)}`);\r\n' +
    '  fs.renameSync(source, target);\r\n' +
    '  return target;\r\n' +
    '}\r\n' +
    '\r\n' +
    'function routeId(pathname, pattern) {\r\n' +
    '  const match = pattern.exec(pathname);\r\n' +
    '  return match ? decodeURIComponent(match[1]) : null;\r\n' +
    '}\r\n' +
    '\r\n' +
    'async function handleLegacyGround(req, res, url) {\r\n' +
    "  if (req.method !== 'POST' || url.pathname !== '/api/object/ground') return false;\r\n" +
    '  const input = await readJsonBody(req);\r\n' +
    '  const result = mutateState(state => {\r\n' +
    '    const object = findObject(state, input.objectId);\r\n' +
    "    if (!object) throw new Error('Object not found.');\r\n" +
    "    if (object.locked) throw new Error('Object is locked.');\r\n" +
    "    const terrain = activeScene(state).objects.find(item => item.type === 'terrain');\r\n" +
    "    if (!terrain) throw new Error('No authoritative terrain exists in the active scene.');\r\n" +
    '    const asset = object.properties?.assetId\r\n' +
    "      ? state.assets.find(item => item.id === object.properties.assetId && item.type === 'model')\r\n" +
    '      : null;\r\n' +
    '    const diagnostics = fitGroundContact({ object, asset, terrain, maxTilt: Number(input.maxTilt || 35) });\r\n' +
    "    addActivity(state, 'placement', `Grounded ${object.name} using ${diagnostics.mode}.`, { objectId: object.id, diagnostics });\r\n" +
    '    return { object, diagnostics };\r\n' +
    '  });\r\n' +
    '  json(res, 200, { ...result.result, state: result.state });\r\n' +
    '  return true;\r\n' +
    '}\r\n' +
    '\r\n' +
    'export async function handleV010Request(req, res) {\r\n' +
    "  const url = new URL(req.url, 'http://127.0.0.1');\r\n" +
    '  try {\r\n' +
    '    if (await handleLegacyGround(req, res, url)) return true;\r\n' +
    "    if (!url.pathname.startsWith('/api/v010/')) return false;\r\n" +
    '\r\n' +
    "    if (req.method === 'GET' && url.pathname === '/api/v010/world') {\r\n" +
    '      const state = readState();\r\n' +
    '      const world = ensureWorld(state);\r\n' +
    '      json(res, 200, {\r\n' +
    '        world,\r\n' +
    '        scene: activeScene(state),\r\n' +
    "        assets: (state.assets || []).filter(item => ['model', 'foliageSpecies', 'foliageFamily', 'biomeRecipe', 'windProfile'].includes(item.type)),\r\n" +
    '        transactions: state.foliageTransactions || [],\r\n' +
    '        runtimeDiagnostics: state.runtimeDiagnostics || {},\r\n' +
    '        state\r\n' +
    '      });\r\n' +
    '      return true;\r\n' +
    '    }\r\n' +
    '\r\n' +
    "    if (req.method === 'PATCH' && url.pathname === '/api/v010/world') {\r\n" +
    '      const input = await readJsonBody(req);\r\n' +
    '      const result = mutateState(state => {\r\n' +
    '        const current = defaultWorldSettings(state.worldV010 || {});\r\n' +
    '        state.worldV010 = defaultWorldSettings({\r\n' +
    '          ...current,\r\n' +
    '          ...input,\r\n' +
    '          time: { ...current.time, ...(input.time || {}) },\r\n' +
    '          lighting: { ...current.lighting, ...(input.lighting || {}) },\r\n' +
    '          atmosphere: { ...current.atmosphere, ...(input.atmosphere || {}) },\r\n' +
    '          sky: { ...current.sky, ...(input.sky || {}) },\r\n' +
    '          clouds: { ...current.clouds, ...(input.clouds || {}) },\r\n' +
    '          weather: { ...current.weather, ...(input.weather || {}) },\r\n' +
    '          updatedAt: now()\r\n' +
    '        });\r\n' +
    '        const derived = applyWorldToScene(activeScene(state), state.worldV010);\r\n' +
    "        addActivity(state, 'world', 'Updated connected time, lighting, atmosphere, sky, clouds, or weather.', { derived });\r\n" +
    '        return { world: state.worldV010, derived };\r\n' +
    '      });\r\n' +
    '      json(res, 200, { ...result.result, state: result.state });\r\n' +
    '      return true;\r\n' +
    '    }\r\n' +
    '\r\n' +
    "    if (req.method === 'POST' && url.pathname === '/api/v010/world/step') {\r\n" +
    '      const input = await readJsonBody(req);\r\n' +
    '      const result = mutateState(state => {\r\n' +
    '        const world = ensureWorld(state);\r\n' +
    '        const seconds = Math.max(0, Math.min(60, Number(input.seconds || 1)));\r\n' +
    '        if (world.time.enabled !== false) {\r\n' +
    '          world.time.hours = ((Number(world.time.hours || 0) + seconds * Number(world.time.timeScale || 0) / 3600) % 24 + 24) % 24;\r\n' +
    '        }\r\n' +
    '        world.updatedAt = now();\r\n' +
    '        const derived = applyWorldToScene(activeScene(state), world);\r\n' +
    '        return { world, derived };\r\n' +
    '      });\r\n' +
    "      const includeFullState = url.searchParams.get('full') === '1';\r\n" +
    '      json(res, 200, {\r\n' +
    '        ...result.result,\r\n' +
    '        runtime: compactWorldRuntime(result.state),\r\n' +
    '        ...(includeFullState ? { state: result.state } : {})\r\n' +
    '      });\r\n' +
    '      return true;\r\n' +
    '    }\r\n' +
    '\r\n' +
    "    if (req.method === 'POST' && url.pathname === '/api/v010/foliage/species') {\r\n" +
    '      const input = await readJsonBody(req);\r\n' +
    '      const result = mutateState(state => {\r\n' +
    '        const source = findModelAsset(state, input.sourceAssetId);\r\n' +
    "        const id = input.id || `foliage-species-${source.id.replace(/^asset-/, '')}-${Date.now().toString(36)}`;\r\n" +
    "        const existing = state.assets.find(item => item.id === id && item.type === 'foliageSpecies');\r\n" +
    '        const species = {\r\n' +
    '          ...(existing || {}),\r\n' +
    '          id,\r\n' +
    "          type: 'foliageSpecies',\r\n" +
    '          name: String(input.name || `${source.name} Species`).slice(0, 120),\r\n' +
    '          sourceAssetId: source.id,\r\n' +
    '          variants: Array.isArray(input.variants) ? input.variants : [],\r\n' +
    '          scaleMin: Number(input.scaleMin ?? 0.85),\r\n' +
    '          scaleMax: Number(input.scaleMax ?? 1.2),\r\n' +
    '          spacing: Number(input.spacing ?? 2.5),\r\n' +
    '          maxSlope: Number(input.maxSlope ?? 42),\r\n' +
    '          rootBurial: Number(input.rootBurial ?? 0.08),\r\n' +
    '          pathExclusion: Number(input.pathExclusion ?? 2.5),\r\n' +
    '          structureExclusion: Number(input.structureExclusion ?? 2),\r\n' +
    '          wind: { strength: Number(input.windStrength ?? 0.35), frequency: Number(input.windFrequency ?? 1) },\r\n' +
    '          lod: {\r\n' +
    '            near: Number(input.lodNear ?? 35),\r\n' +
    '            far: Number(input.lodFar ?? 100),\r\n' +
    '            impostor: Number(input.impostorDistance ?? 160)\r\n' +
    '          },\r\n' +
    '          collision: Boolean(input.collision),\r\n' +
    '          navigationBlocker: Boolean(input.navigationBlocker),\r\n' +
    '          seasonal: true,\r\n' +
    "          validation: { state: 'valid', warnings: [] },\r\n" +
    '          createdAt: existing?.createdAt || now(),\r\n' +
    '          updatedAt: now()\r\n' +
    '        };\r\n' +
    '        if (existing) Object.assign(existing, species); else state.assets.unshift(species);\r\n' +
    "        addActivity(state, 'foliage', `Created foliage species: ${species.name}`, { speciesId: id, sourceAssetId: source.id });\r\n" +
    '        return species;\r\n' +
    '      });\r\n' +
    '      json(res, 201, { species: result.result, state: result.state });\r\n' +
    '      return true;\r\n' +
    '    }\r\n' +
    '\r\n' +
    "    if (req.method === 'POST' && url.pathname === '/api/v010/foliage/family') {\r\n" +
    '      const input = await readJsonBody(req);\r\n' +
    '      const result = mutateState(state => {\r\n' +
    "        const speciesIds = (input.speciesIds || []).filter(id => state.assets.some(item => item.id === id && item.type === 'foliageSpecies'));\r\n" +
    "        if (!speciesIds.length) throw new Error('A foli"... 10589 more characters
  
      at TestContext.<anonymous> (file:///D:/a/OmniForge/OmniForge/tests/phase0-celestial-stabilization.test.mjs:99:10)
      at Test.runInAsyncScope (node:async_hooks:227:14)
      at Test.run (node:internal/test_runner/test:1325:25)
      at Test.processPendingSubtests (node:internal/test_runner/test:911:18)
      at Test.postRun (node:internal/test_runner/test:1465:19)
      at Test.run (node:internal/test_runner/test:1390:12)
      at async Test.processPendingSubtests (node:internal/test_runner/test:911:7) {
    generatedMessage: true,
    code: 'ERR_ASSERTION',
    actual: "import fs from 'node:fs';\r\nimport path from 'node:path';\r\nimport {\r\n  readState,\r\n  mutateState,\r\n  addActivity,\r\n  activeScene,\r\n  findObject,\r\n  createSceneObject,\r\n  ASSET_ROOT\r\n} from './state-store.mjs';\r\nimport {\r\n  defaultWorldSettings,\r\n  applyWorldToScene,\r\n  fitGroundContact,\r\n  generateFoliagePlacements\r\n} from './v010-systems.mjs';\r\n\r\nconst now = () => new Date().toISOString();\r\n\r\nfunction json(res, status, payload) {\r\n  const responseBody = JSON.stringify(payload, null, 2);\r\n  res.writeHead(status, {\r\n    'content-type': 'application/json; charset=utf-8',\r\n    'content-length': Buffer.byteLength(responseBody),\r\n    'cache-control': 'no-store'\r\n  });\r\n  res.end(responseBody);\r\n}\r\n\r\n\r\nfunction compactWorldRuntime(state) {\r\n  const scene = activeScene(state);\r\n  return {\r\n    engineRevision: Number(state.engine?.revision || 0),\r\n    sceneId: scene.id,\r\n    settings: structuredClone(scene.settings || {}),\r\n    celestialObjects: scene.objects\r\n      .filter(object => object.type === 'directionalLight' || object.properties?.celestialRole)\r\n      .map(object => ({\r\n        id: object.id,\r\n        type: object.type,\r\n        visible: object.visible !== false,\r\n        transform: structuredClone(object.transform),\r\n        properties: structuredClone(object.properties || {})\r\n      }))\r\n  };\r\n}\r\n\r\nasync function readJsonBody(req) {\r\n  const chunks = [];\r\n  let size = 0;\r\n  for await (const chunk of req) {\r\n    size += chunk.length;\r\n    if (size > 20_000_000) throw new Error('The v0.10 request body exceeds the 20 MB safety limit.');\r\n    chunks.push(chunk);\r\n  }\r\n  return chunks.length ? JSON.parse(Buffer.concat(chunks).toString('utf8')) : {};\r\n}\r\n\r\nfunction ensureWorld(state) {\r\n  state.worldV010 = defaultWorldSettings(state.worldV010 || {});\r\n  applyWorldToScene(activeScene(state), state.worldV010);\r\n  return state.worldV010;\r\n}\r\n\r\nfunction findModelAsset(state, assetId) {\r\n  const asset = (state.assets || []).find(item => item.id === assetId && item.type === 'model');\r\n  if (!asset) throw new Error('Model asset not found.');\r\n  return asset;\r\n}\r\n\r\nfunction assetUsages(state, assetId) {\r\n  const sceneUsages = [];\r\n  for (const scene of state.scenes || []) {\r\n    for (const object of scene.objects || []) {\r\n      if (object.properties?.assetId === assetId) {\r\n        sceneUsages.push({ sceneId: scene.id, sceneName: scene.name, objectId: object.id, objectName: object.name });\r\n      }\r\n    }\r\n  }\r\n  const dependencies = (state.assets || [])\r\n    .filter(item => item.id !== assetId && (\r\n      item.sourceAssetId === assetId\r\n      || item.canonicalAssetId === assetId\r\n      || item.baseAssetId === assetId\r\n      || item.sourceModelAssetId === assetId\r\n      || item.sourceAssetIds?.includes?.(assetId)\r\n    ))\r\n    .map(item => ({ id: item.id, type: item.type, name: item.name }));\r\n  return { sceneUsages, dependencies };\r\n}\r\n\r\nfunction moveManagedAssetToTrash(assetId) {\r\n  const modelsRoot = path.resolve(ASSET_ROOT, 'models');\r\n  const source = path.resolve(modelsRoot, assetId);\r\n  if (!source.startsWith(`${modelsRoot}${path.sep}`)) throw new Error('Refusing to delete outside the managed model directory.');\r\n  if (!fs.existsSync(source)) return null;\r\n  const trashRoot = path.resolve(ASSET_ROOT, '.trash');\r\n  fs.mkdirSync(trashRoot, { recursive: true });\r\n  const target = path.join(trashRoot, `${assetId}-${Date.now().toString(36)}`);\r\n  fs.renameSync(source, target);\r\n  return target;\r\n}\r\n\r\nfunction routeId(pathname, pattern) {\r\n  const match = pattern.exec(pathname);\r\n  return match ? decodeURIComponent(match[1]) : null;\r\n}\r\n\r\nasync function handleLegacyGround(req, res, url) {\r\n  if (req.method !== 'POST' || url.pathname !== '/api/object/ground') return false;\r\n  const input = await readJsonBody(req);\r\n  const result = mutateState(state => {\r\n    const object = findObject(state, input.objectId);\r\n    if (!object) throw new Error('Object not found.');\r\n    if (object.locked) throw new Error('Object is locked.');\r\n    const terrain = activeScene(state).objects.find(item => item.type === 'terrain');\r\n    if (!terrain) throw new Error('No authoritative terrain exists in the active scene.');\r\n    const asset = object.properties?.assetId\r\n      ? state.assets.find(item => item.id === object.properties.assetId && item.type === 'model')\r\n      : null;\r\n    const diagnostics = fitGroundContact({ object, asset, terrain, maxTilt: Number(input.maxTilt || 35) });\r\n    addActivity(state, 'placement', `Grounded ${object.name} using ${diagnostics.mode}.`, { objectId: object.id, diagnostics });\r\n    return { object, diagnostics };\r\n  });\r\n  json(res, 200, { ...result.result, state: result.state });\r\n  return true;\r\n}\r\n\r\nexport async function handleV010Request(req, res) {\r\n  const url = new URL(req.url, 'http://127.0.0.1');\r\n  try {\r\n    if (await handleLegacyGround(req, res, url)) return true;\r\n    if (!url.pathname.startsWith('/api/v010/')) return false;\r\n\r\n    if (req.method === 'GET' && url.pathname === '/api/v010/world') {\r\n      const state = readState();\r\n      const world = ensureWorld(state);\r\n      json(res, 200, {\r\n        world,\r\n        scene: activeScene(state),\r\n        assets: (state.assets || []).filter(item => ['model', 'foliageSpecies', 'foliageFamily', 'biomeRecipe', 'windProfile'].includes(item.type)),\r\n        transactions: state.foliageTransactions || [],\r\n        runtimeDiagnostics: state.runtimeDiagnostics || {},\r\n        state\r\n      });\r\n      return true;\r\n    }\r\n\r\n    if (req.method === 'PATCH' && url.pathname === '/api/v010/world') {\r\n      const input = await readJsonBody(req);\r\n      const result = mutateState(state => {\r\n        const current = defaultWorldSettings(state.worldV010 || {});\r\n        state.worldV010 = defaultWorldSettings({\r\n          ...current,\r\n          ...input,\r\n          time: { ...current.time, ...(input.time || {}) },\r\n          lighting: { ...current.lighting, ...(input.lighting || {}) },\r\n          atmosphere: { ...current.atmosphere, ...(input.atmosphere || {}) },\r\n          sky: { ...current.sky, ...(input.sky || {}) },\r\n          clouds: { ...current.clouds, ...(input.clouds || {}) },\r\n          weather: { ...current.weather, ...(input.weather || {}) },\r\n          updatedAt: now()\r\n        });\r\n        const derived = applyWorldToScene(activeScene(state), state.worldV010);\r\n        addActivity(state, 'world', 'Updated connected time, lighting, atmosphere, sky, clouds, or weather.', { derived });\r\n        return { world: state.worldV010, derived };\r\n      });\r\n      json(res, 200, { ...result.result, state: result.state });\r\n      return true;\r\n    }\r\n\r\n    if (req.method === 'POST' && url.pathname === '/api/v010/world/step') {\r\n      const input = await readJsonBody(req);\r\n      const result = mutateState(state => {\r\n        const world = ensureWorld(state);\r\n        const seconds = Math.max(0, Math.min(60, Number(input.seconds || 1)));\r\n        if (world.time.enabled !== false) {\r\n          world.time.hours = ((Number(world.time.hours || 0) + seconds * Number(world.time.timeScale || 0) / 3600) % 24 + 24) % 24;\r\n        }\r\n        world.updatedAt = now();\r\n        const derived = applyWorldToScene(activeScene(state), world);\r\n        return { world, derived };\r\n      });\r\n      const includeFullState = url.searchParams.get('full') === '1';\r\n      json(res, 200, {\r\n        ...result.result,\r\n        runtime: compactWorldRuntime(result.state),\r\n        ...(includeFullState ? { state: result.state } : {})\r\n      });\r\n      return true;\r\n    }\r\n\r\n    if (req.method === 'POST' && url.pathname === '/api/v010/foliage/species') {\r\n      const input = await readJsonBody(req);\r\n      const result = mutateState(state => {\r\n        const source = findModelAsset(state, input.sourceAssetId);\r\n        const id = input.id || `foliage-species-${source.id.replace(/^asset-/, '')}-${Date.now().toString(36)}`;\r\n        const existing = state.assets.find(item => item.id === id && item.type === 'foliageSpecies');\r\n        const species = {\r\n          ...(existing || {}),\r\n          id,\r\n          type: 'foliageSpecies',\r\n          name: String(input.name || `${source.name} Species`).slice(0, 120),\r\n          sourceAssetId: source.id,\r\n          variants: Array.isArray(input.variants) ? input.variants : [],\r\n          scaleMin: Number(input.scaleMin ?? 0.85),\r\n          scaleMax: Number(input.scaleMax ?? 1.2),\r\n          spacing: Number(input.spacing ?? 2.5),\r\n          maxSlope: Number(input.maxSlope ?? 42),\r\n          rootBurial: Number(input.rootBurial ?? 0.08),\r\n          pathExclusion: Number(input.pathExclusion ?? 2.5),\r\n          structureExclusion: Number(input.structureExclusion ?? 2),\r\n          wind: { strength: Number(input.windStrength ?? 0.35), frequency: Number(input.windFrequency ?? 1) },\r\n          lod: {\r\n            near: Number(input.lodNear ?? 35),\r\n            far: Number(input.lodFar ?? 100),\r\n            impostor: Number(input.impostorDistance ?? 160)\r\n          },\r\n          collision: Boolean(input.collision),\r\n          navigationBlocker: Boolean(input.navigationBlocker),\r\n          seasonal: true,\r\n          validation: { state: 'valid', warnings: [] },\r\n          createdAt: existing?.createdAt || now(),\r\n          updatedAt: now()\r\n        };\r\n        if (existing) Object.assign(existing, species); else state.assets.unshift(species);\r\n        addActivity(state, 'foliage', `Created foliage species: ${species.name}`, { speciesId: id, sourceAssetId: source.id });\r\n        return species;\r\n      });\r\n      json(res, 201, { species: result.result, state: result.state });\r\n      return true;\r\n    }\r\n\r\n    if (req.method === 'POST' && url.pathname === '/api/v010/foliage/family') {\r\n      const input = await readJsonBody(req);\r\n      const result = mutateState(state => {\r\n        const speciesIds = (input.speciesIds || []).filter(id => state.assets.some(item => item.id === id && item.type === 'foliageSpecies'));\r\n        if (!speciesIds.length) throw new Error('A foli"... 10589 more characters,
    expected: /world-read-migration/,
    operator: 'match',
    diff: 'simple'
  }


--- STDERR ---
Automated tests failed.

