# Managed game projects

OmniForge stores project-specific source files under this directory. The engine itself remains general-purpose.

Codex should prefer the structured MCP scene tools for objects and world data. Source files belong here when a new runtime system, script, shader, data schema, or exported game module genuinely requires them.
